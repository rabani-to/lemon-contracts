import { expect } from "chai"

describe("SimpleDonationModule", function () {
  describe("Deployment", function () {
    it("Should set the right unlockTime", async function () {
      expect(1).to.equal(1)
    })
  })
})
